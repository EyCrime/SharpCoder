using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class CodeSet
{
    public static List<string> Keywords = new List<string>()
    {
        "public", "private", "protected", "static", "void", "int", "string", "float", "double", "bool",
        "if", "else", "for", "while", "return", "new", "class", "using", "namespace", 
        "Console", "WriteLine", "ReadKey", "Clear"
    };
}
